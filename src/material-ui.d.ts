/**
 * config or add new property for theme MUI
 */

// declare module "@mui/material/styles" {
//   interface CustomTheme extends Theme {
//     app: {
//       appBarHeight: string;
//     };
//   }
//   // allow configuration using `createTheme`
//   interface CustomThemeOptions extends ThemeOptions {
//     app?: {
//       appBarHeight?: string;
//     };
//   }
//   export function extendTheme(options?: CustomThemeOptions): CustomTheme;
// }
// // Combine the default theme options with your custom properties
// export type CustomTheme = ThemeOptions & CustomThemeOptions;

// import { ThemeOptions } from "@mui/material/styles";

// // Define your custom properties here
// interface CustomThemeOptions {
//   app: {
//     myProperty: string;
//     // Add other custom properties as needed
//   };
// }

// // Combine the default theme options with your custom properties
// export type CustomTheme = ThemeOptions & CustomThemeOptions;
